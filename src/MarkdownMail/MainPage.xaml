<Page
    x:Class="Walterlv.MarkdownMail.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Walterlv.MarkdownMail"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView DisplayMode="Inline" IsPaneOpen="True">
            <SplitView.Pane>
                <StackPanel Margin="8">
                    <TextBox x:Name="UserNameBox" Margin="8" />
                    <PasswordBox x:Name="PasswordBox" Margin="8"/>
                    <Button Content="收邮件" Margin="8" Click="ReceiveButton_Click"/>
                </StackPanel>
            </SplitView.Pane>
            <SplitView.Content>
                <TextBlock x:Name="TestTextBlock" Margin="8">
                    
                </TextBlock>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
